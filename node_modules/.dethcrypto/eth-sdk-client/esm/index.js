import { Contract } from 'ethers';
import mainnet_balancer_vault_abi from '../../../../eth-sdk/abis/mainnet/balancer/vault.json';
export function getContract(address, abi, defaultSignerOrProvider) {
    return new Contract(address, abi, defaultSignerOrProvider);
}
export function getMainnetSdk(defaultSignerOrProvider) {
    return {
        "balancer": {
            "vault": getContract('0xba12222222228d8ba445958a75a0704d566bf2c8', mainnet_balancer_vault_abi, defaultSignerOrProvider),
        },
    };
}
