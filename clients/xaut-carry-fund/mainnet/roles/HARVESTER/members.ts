// Harvest agent EOA to be supplied before the role is assigned on-chain.
// Role membership is granted by a separate `assignRoles` transaction — it is not
// part of the permissions payload produced by `yarn apply` / `yarn apply:export`.
export default []
